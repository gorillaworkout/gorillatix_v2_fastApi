export type TicketViewerProps = {
  id: string;
  eventName: string;
  date: string;
  time: string;
  location: string;
  venue: string;
  quantity: number;
  totalPrice: number;
  status: string;
  customerName: string;
};